\subsection{Numerical solution}

We can now proceed to solve the system of equations ${\vec{P}} = f({\vec{u}})$ numerically.
To do so, we will use two different combined methods:

\begin{itemize}
    \item Euler and Newton-Raphson
    \item Euler and modified Newton-Raphson
\end{itemize}

% Just as a review of the methods, we report in here the algorithms for the tree methods applied to a multidimensional problem.

% \subsubsection{Euler}

% \begin{equation}
%     % TODO: check Euler algorithm
%     {\vec{u}_{n+1}} = {\vec{u}_n} + [K_t]^{-1} * {\vec{P_{ext,n}}}
% \end{equation}

% \subsubsection{Newton-Raphson}

% \subsubsection{Modified Newton-Raphson}